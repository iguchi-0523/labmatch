/**
 * 取り込みの「完了」判定に使うしきい値。
 * ingest-next.ts / progress-report.ts / lib/stats.ts の 3 か所で同じ値を使う。
 */

/**
 * その機関を取り込み済みとみなす最小ラボ数（子センター合算）。
 *
 * 2026-07-23 に 50 から 1 へ変更。残っていたのは産総研・NIMS・KEK・JAXA のような
 * 国立研究機関と、Kavli IPMU（7 labs）・会津大学（35 labs）のように OpenAlex 側の
 * PI 数がそもそも 50 に届かない機関で、50 を満たす日は来ない。ingest は 1 機関 1 回で
 * その分野の PI を出し尽くす単発処理なので、「1 件でもラボが付いた＝出し尽くした」を
 * 完了の基準にする。ラボが 0 のままなら未着手として次の cron が拾う。
 */
export const DONE_MIN_LABS = 1;

/**
 * 完了扱いではあるが規模が小さく、分野追加での追い取り込み余地がある機関の目安。
 * 完了判定には使わず、進捗レポートの注記だけに使う。
 */
export const SMALL_INSTITUTION_LABS = 50;
